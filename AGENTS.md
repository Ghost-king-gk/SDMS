# 项目开发规范

## 1. 项目定位

本仓库对应 `Requests/` 中的《Python 日程管理系统设计（等级 A）》课程需求。目标是实现一个 Python 图形化日程管理系统，覆盖日程的添加、查看、修改、删除、按日期筛选，以及对非法操作和异常情况的友好提示。

`Requests/` 是需求输入目录，内容只读，不在开发过程中直接修改。任何需求变更应在讨论后补充到项目文档，并保留与原始需求的对应关系。

当前仓库只完成工程骨架和开发规范初始化，暂不包含业务实现代码。

## 2. 目录约定

```text
.
├── AGENT.md                 # 兼容入口；规范以 AGENTS.md 为准
├── AGENTS.md                # 本项目智能体和开发规范
├── README.md                # 环境、构建、运行、测试和交付说明
├── Requests/                # 原始需求材料，只读
├── pyproject.toml           # uv、测试、格式化和类型检查配置
├── uv.lock                  # uv 生成的可复现依赖锁定文件
├── src/                     # 全部 Python 源代码
│   ├── schedule_management/ # 应用包，待实现
│   └── lib/                 # 作业要求的第三方包随源代码提交位置
├── tests/                   # 自动化测试
├── docs/
│   ├── design/              # 每位成员的 Word 设计文档
│   ├── presentation/        # 组级 PowerPoint 演示文档
│   ├── figures/             # 流程图、示意图、统计图表
│   └── screenshots/         # 测试和运行截图
└── scripts/                 # 可复用的项目维护脚本；当前不放业务代码
```

约定如下：

- 业务源代码只能放在 `src/`，不要把可运行模块放在仓库根目录。
- 优先使用 Python 标准库；若使用第三方包，依赖版本由 `pyproject.toml` 和 `uv.lock` 管理。若课程提交要求将第三方包随源代码提交，放到 `src/lib/`，同时记录来源、版本、许可证和引入原因，不要提交整个 `.venv/`。
- 测试放在 `tests/`，并按源代码模块组织；测试数据和截图放到 `docs/` 对应目录。
- 设计文档和演示文档是交付物，不把它们放进 `src/`。
- `.venv/`、`.uv-cache/`、缓存、构建产物和本地配置不得提交。

## 3. Python 与 uv 规范

- Python 最低版本为 3.12，以仓库根目录的 `.python-version` 为默认版本提示。
- 使用 uv 管理虚拟环境、依赖解析和锁文件，禁止为了本项目向全局 Python 安装依赖。
- 推荐命令：

  ```powershell
  uv venv --python 3.12
  uv sync --dev
  uv run python --version
  uv run pytest
  uv run ruff check .
  uv run mypy src
  ```

- 新增或删除依赖后必须重新生成并审阅 `uv.lock`。
- 当首个业务包加入时，再将 `pyproject.toml` 从当前的非打包项目配置调整为可安装的 `src` 布局，并补充明确的应用入口；本次初始化不预先写入入口代码。

## 4. 实现约束

- GUI 框架在实现阶段从 Tkinter 或 PyQt 中二选一，并在设计文档中记录选型理由；本次骨架不预装 GUI 框架。
- 日期和时间统一使用 `datetime` 及明确的格式化/解析策略，避免用不透明字符串进行核心比较。
- 外部输入和界面输入必须校验；错误应转换为用户可理解的提示，不向界面泄露无关的堆栈信息。
- 日程的新增、查询、修改、删除都要有可测试的边界条件，尤其是空标题、非法日期时间、未选中日程、重复操作和删除确认。
- 新增公共函数、类或模块时补充类型标注和简短文档说明；避免隐式全局状态。
- 业务实现完成后再补充启动入口、持久化策略、测试夹具和截图，不以占位代码冒充完成状态。

## 5. 测试与质量门禁

提交前至少运行：

```powershell
uv run ruff check .
uv run mypy src
uv run pytest
uv run python -m compileall src
```

测试应覆盖正常流程和错误流程，并在报告中保留可复核的测试结果。GUI 测试需要记录环境、操作步骤、预期结果、实际结果和必要截图。

## 6. Git 规范

- 默认分支为 `main`；功能开发使用 `feature/<简短主题>`，修复使用 `fix/<简短主题>`，文档使用 `docs/<简短主题>`。
- 提交信息采用 Conventional Commits：`feat`、`fix`、`docs`、`test`、`refactor`、`chore` 等类型，主题简短明确。
- 提交前先查看 `git status` 和 `git diff --staged`，不要未经审阅直接使用宽泛的 `git add .`。
- 不提交密钥、个人环境文件、虚拟环境、缓存、构建产物或未经许可的外部素材。
- 任何重命名、删除或批量替换都要先确认目标范围；不要使用破坏性 Git 命令覆盖他人改动。

## 7. 课程交付要求

- 每位成员一份 Word 设计文档：写清成员分工和个人完成部分，包含流程图、示意图、统计图表、测试截图等，正文不少于 20 页（附录不计入）。
- 每组一份 PowerPoint：包含图表和实现方法，首页列出按贡献大小排序的成员姓名与学号，页内标注具体贡献，不少于 20 页。
- README 必须说明环境配置、编译/检查、运行、测试和结果。
- 不得自行更换题目，不得使用 Python 以外的实现语言，不得以缺少基本功能或缺少交付材料的状态提交。

## 8. 智能体协作约定

- 修改前先阅读 `Requests/`、`README.md` 和本文件，明确本次改动的范围。
- 只实现用户明确授权的范围；当前初始化任务不写业务代码。
- 保留已有用户改动，完成后报告新增文件、验证命令和仍待人工完成的交付物。

# `src/lib/` 第三方包目录

课程需求要求：如果使用第三方包并将其随源代码提交，应放在 `src/lib/`。当前尚未选定 GUI 框架，也没有需要放入本目录的第三方包。

实现阶段请同时遵守：

- 使用 `pyproject.toml` 声明依赖，使用 `uv.lock` 锁定解析结果；
- 只提交确有必要、许可明确且符合课程要求的文件，不提交整个虚拟环境；
- 在项目文档中记录包名、版本、来源、许可证和引入原因。

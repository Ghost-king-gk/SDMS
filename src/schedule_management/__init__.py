"""Python 图形化日程管理系统。

本包按分层组织，依赖方向自上而下，不允许反向依赖：

- ``domain``：日程的含义与有效性规则，不依赖界面和存储；
- ``application``：新增、按日期查询、修改、删除等用例的编排；
- ``infrastructure``：持久化等外部适配器，实现应用层定义的契约；
- ``ui``：图形界面与交互，只调用应用层用例。

这些子包当前尚未实现，按开发计划逐个加入。本文件只声明包边界与版本号，
不放业务逻辑，也不要在这里导入重模块或执行有副作用的操作。
"""

__version__ = "0.1.0"
